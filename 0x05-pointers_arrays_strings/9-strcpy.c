#include "main.h"
#include <stdio.h>

/**
 * *_strcpy - | starts the file
 * @dest: - | var
 * @src: - | var
 * Return: - | ends the file
 */

void *_strcpy(char *dest, char *src)
{

int y = 0;

if (s[0] != '\0')
{

while (s[y] != '\0')
{
y++;
}

while (y >= 0)
{
if (s[y] != '\0')
{
putchar(s[y]);
y--;
}
else
{
y--;
}
}

}
putchar('\n');
}
