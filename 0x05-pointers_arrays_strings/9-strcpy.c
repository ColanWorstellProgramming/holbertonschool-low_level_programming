#include "main.h"
#include <stdio.h>

/**
 * *_strcpy - | starts the file
 * @dest: - | var
 * @src: - | var
 * Return: - | ends the file
 */

char *_strcpy(char *dest, char *src)
{

int i = 0;
char x[];

while (src[i] != '\0')
{
x[i] = src[i];
i++;
}

dest = x[];

return (dest);
}
