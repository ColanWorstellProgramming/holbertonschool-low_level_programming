#include "main.h"
#include <stdio.h>

/**
 * isupper - | starts the file
 * @c: - | var
 * Return: - | ends the file
 */

int _isupper(int c)
{

if (c > 64 && c < 91)
{
return (1);
}
else
{
return (0);
}

}
