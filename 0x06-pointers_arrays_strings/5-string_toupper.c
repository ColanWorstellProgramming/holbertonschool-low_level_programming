#include "holberton.h"

/**
 * string_toupper - | starts the file
 * @*: - | var
 * Return: - | ends the file
 */

char *string_toupper(char *)
{

int i = 0;

while (*[i] != '\0')
{
if (*[i] > 96 && *[i] < 123)
{
*[i] = *[i] - 32;
}
i++;
}

}
